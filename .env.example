# This is the template for the environment variables
ENV=test
OVERWRITE_URL=https://the-internet.herokuapp.com
TAG=@smoke

# Browser settings
BROWSER=chrome
HEADLESS=false

# BrowserStack (required when running: npm run test:browserstack)
BROWSERSTACK_USERNAME=
BROWSERSTACK_ACCESS_KEY=
BUILD_NAME=local
